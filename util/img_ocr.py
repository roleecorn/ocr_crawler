from pathlib import Path


def img_ocr(img: Path, x: int, y: int, w: int, h: int) -> str:
    """
    輸入一張圖片與對象範圍，回傳範圍內的文字
    - x: X座標
    - y: Y座標
    - w: 寬度
    - h: 高度
    """
    text = ""

    return text


if __name__ == "__main__":
    print(img_ocr())
